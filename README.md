# 📒 Python CLI Contact Book

A simple, efficient, and persistent contact management system built with **Python**. This console-based application allows users to create, read, update, and delete (CRUD) contact information, with all data stored locally in a JSON file.

## 🚀 Features

* **Create Contacts:** Save names, ages, emails, and mobile numbers.
* **Persistent Storage:** Uses `contacts.json` to save data automatically upon exiting; your data remains safe even after closing the app.
* **Search Functionality:** Case-insensitive search to find contacts by partial name.
* **Update & Delete:** Easily modify existing details or remove contacts.
* **Validation:** Includes basic input validation for age (integer check).
* **Overview:** View a specific contact or list all saved contacts at once.

## 🛠️ Technologies Used

* **Language:** Python 3.x
* **Libraries:** `json`, `os` (Standard Library - No external installation required).

